<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Camera permission for photo puzzle -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- Vibration permission for game feedback -->
    <uses-permission android:name="android.permission.VIBRATE" /> <!-- Storage permission for gallery images (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameHub"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activities.PianoTilesActivity"
            android:exported="false" />
        <activity
            android:name=".activities.NeonDashActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ReactionTimeActivity"
            android:exported="false" />
        <activity
            android:name=".activities.RapidTapActivity"
            android:exported="false" />
        <activity
            android:name=".activities.StatsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.LeaderboardActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ProfileSetupActivity"
            android:exported="false" />
        <activity
            android:name=".activities.WordQuestResultActivity"
            android:exported="false" />
        <activity
            android:name=".activities.WordQuestGameActivity"
            android:exported="false" />
        <activity
            android:name=".activities.WordQuestActivity"
            android:exported="false" /> <!-- Splash Screen - Launcher -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.GameHub.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" /> <!-- Memory Match Game -->
        <activity
            android:name=".activities.MemoryMatchActivity"
            android:exported="false"
            android:screenOrientation="portrait" /> <!-- Quiz Game Activities -->
        <activity
            android:name=".activities.QuizStartActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.QuizActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.QuizResultActivity"
            android:exported="false"
            android:screenOrientation="portrait" /> <!-- Photo Puzzle Game (FIXED: Changed from PuzzleGameActivity to PhotoPuzzleActivity) -->
        <activity
            android:name=".activities.PhotoPuzzleActivity"
            android:exported="false"
            android:screenOrientation="portrait" /> <!-- File Provider for Camera Images -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>